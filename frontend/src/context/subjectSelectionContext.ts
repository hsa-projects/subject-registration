import React from "react";

/*
 * Saves the current subject selection of the user throughout the application.
 */
const SubjectSelectionContext = React.createContext(null);

export default SubjectSelectionContext;
